# Project-wide Gradle settings.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8

# AndroidX and Kotlin configuration
android.useAndroidX=true
kotlin.code.style=official

# Modern Android defaults (Deprecations fixed)
android.nonTransitiveRClass=true
# android.dependency.excludeLibraryComponentsFromConstraints=true is replaced by:
android.dependency.useConstraints=false
# Other deprecated flags removed to use modern defaults:
# android.defaults.buildfeatures.resvalues=false (default)
# android.sdk.defaultTargetSdkToCompileSdkIfUnset=true (default)
# android.enableAppCompileTimeRClass=true (default)
# android.usesSdkInManifest.disallowed=true (default)
# android.r8.optimizedResourceShrinking=true (default)
# android.builtInKotlin=true (default)
# android.newDsl=true (default)
